# 项目管理模块 - 客户化定制执行计划

## 📋 总体目标

实现 10 项自定义功能，提升项目管理模块的用户体验和业务适配性。

---

## 🎯 执行计划（分 3 个阶段）

### 阶段 1：后端接口准备（预计 30 分钟）

#### 1.1 新增 SysUserController 接口
**文件**: `ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/SysUserController.java`

```java
/**
 * 根据岗位编码查询用户列表
 */
@GetMapping("/listByPost")
public AjaxResult listByPost(String postCode) {
    SysUser user = new SysUser();
    List<SysUser> list = userService.selectUserList(user);
    // 过滤出指定岗位的用户
    List<SysUser> filteredList = list.stream()
        .filter(u -> {
            List<SysPost> posts = u.getPosts();
            return posts != null && posts.stream()
                .anyMatch(p -> postCode.equals(p.getPostCode()));
        })
        .collect(Collectors.toList());
    return success(filteredList);
}
```

#### 1.2 新增 SecondaryRegionController 接口
**文件**: `ruoyi-project/src/main/java/com/ruoyi/project/controller/SecondaryRegionController.java`

```java
/**
 * 根据一级区域查询二级区域列表
 */
@GetMapping("/listByPrimary")
public AjaxResult listByPrimary(String regionDictValue) {
    List<SecondaryRegion> list = secondaryRegionService.selectSecondaryRegionList(
        new SecondaryRegion().setRegionDictValue(regionDictValue)
    );
    return success(list);
}
```

#### 1.3 新增 CustomerController 接口
**文件**: `ruoyi-project/src/main/java/com/ruoyi/project/controller/CustomerController.java`

```java
/**
 * 根据客户ID查询联系人列表
 */
@GetMapping("/contacts/{customerId}")
public AjaxResult getContactsByCustomerId(@PathVariable Long customerId) {
    // 假设已有 CustomerContact 相关服务
    List<CustomerContact> list = customerContactService.selectCustomerContactList(
        new CustomerContact().setCustomerId(customerId)
    );
    return success(list);
}
```

#### 1.4 新增 ProjectController 接口
**文件**: `ruoyi-project/src/main/java/com/ruoyi/project/controller/ProjectController.java`

```java
/**
 * 项目名称自动完成
 */
@GetMapping("/autocomplete")
public AjaxResult autocomplete(String projectName) {
    if (StringUtils.isEmpty(projectName)) {
        return success(new ArrayList<>());
    }
    List<Project> list = projectService.selectProjectList(
        new Project().setProjectName(projectName)
    );
    List<String> names = list.stream()
        .map(Project::getProjectName)
        .distinct()
        .limit(10)
        .collect(Collectors.toList());
    return success(names);
}
```

#### 1.5 修改 ProjectServiceImpl - 项目编号自动生成
**文件**: `ruoyi-project/src/main/java/com/ruoyi/project/service/impl/ProjectServiceImpl.java`

```java
@Override
public int insertProject(Project project) {
    // 自动生成项目编号
    if (StringUtils.isEmpty(project.getProjectCode())) {
        String projectCode = generateProjectCode(project);
        project.setProjectCode(projectCode);
    }
    project.setCreateTime(DateUtils.getNowDate());
    return projectMapper.insertProject(project);
}

/**
 * 生成项目编号
 * 格式：{行业代码}-{一级区域代码}-{二级区域代码}{简称}-{年份}
 */
private String generateProjectCode(Project project) {
    StringBuilder code = new StringBuilder();
    
    // 行业代码（需要从字典获取）
    String industryCode = project.getIndustryCode();
    if (StringUtils.isEmpty(industryCode)) {
        industryCode = project.getIndustry(); // 如果没有单独的行业代码，使用行业字段
    }
    code.append(industryCode != null ? industryCode : "");
    code.append("-");
    
    // 一级区域代码
    code.append(project.getRegion() != null ? project.getRegion() : "");
    code.append("-");
    
    // 二级区域代码 + 简称
    code.append(project.getRegionCode() != null ? project.getRegionCode() : "");
    code.append(project.getShortName() != null ? project.getShortName() : "");
    code.append("-");
    
    // 立项年份
    code.append(project.getEstablishedYear() != null ? project.getEstablishedYear() : "");
    
    return code.toString();
}
```

---

### 阶段 2：前端 API 层准备（预计 10 分钟）

**文件**: `ruoyi-ui/src/api/project/project.js`

```javascript
// 获取所有用户列表（用于参与人员多选）
export function getAllUsers() {
  return request({
    url: '/system/user/list',
    method: 'get',
    params: { pageNum: 1, pageSize: 10000 }
  })
}

// 根据岗位编码获取用户列表
export function getUsersByPostCode(postCode) {
  return request({
    url: '/system/user/listByPost',
    method: 'get',
    params: { postCode }
  })
}

// 根据一级区域获取二级区域列表
export function getSecondaryRegionsByPrimary(regionDictValue) {
  return request({
    url: '/project/secondaryRegion/listByPrimary',
    method: 'get',
    params: { regionDictValue }
  })
}

// 根据客户ID获取联系人列表
export function getContactsByCustomerId(customerId) {
  return request({
    url: '/project/customer/contacts/' + customerId,
    method: 'get'
  })
}

// 项目名称自动完成
export function autocompleteProjectName(projectName) {
  return request({
    url: '/project/project/autocomplete',
    method: 'get',
    params: { projectName }
  })
}
```

---

### 阶段 3：前端页面实现（预计 60 分钟）

#### 3.1 修改查询表单
**位置**: `ruoyi-ui/src/views/project/project/index.vue` - 查询区域

**关键修改点**:
1. 项目名称改为 autocomplete
2. 项目部门改为树形选择器
3. 一级区域、二级区域改为下拉框（字典）
4. 项目经理、市场经理改为下拉框（按岗位过滤）

#### 3.2 修改新增/编辑表单
**位置**: `ruoyi-ui/src/views/project/project/index.vue` - 对话框表单

**关键修改点**:

**1. 项目编号自动生成**
```vue
<el-form-item label="项目编号" prop="projectCode">
  <el-input v-model="form.projectCode" disabled placeholder="保存时自动生成"/>
</el-form-item>
```

**2. 行业下拉框**
```vue
<el-form-item label="行业" prop="industry">
  <el-select v-model="form.industry" placeholder="请选择行业">
    <el-option
      v-for="dict in dict.type.industry"
      :key="dict.value"
      :label="dict.label"
      :value="dict.value"
    />
  </el-select>
</el-form-item>
```

**3. 一级区域 + 二级区域联动**
```vue
<el-form-item label="一级区域" prop="region">
  <el-select v-model="form.region" placeholder="请选择一级区域" @change="handleRegionChange">
    <el-option
      v-for="dict in dict.type.sys_yjqy"
      :key="dict.value"
      :label="dict.label"
      :value="dict.value"
    />
  </el-select>
</el-form-item>

<el-form-item label="二级区域" prop="regionCode">
  <el-select v-model="form.regionCode" placeholder="请选择二级区域" :disabled="!form.region">
    <el-option
      v-for="item in secondaryRegionList"
      :key="item.provinceCode"
      :label="item.provinceName"
      :value="item.provinceCode"
    />
  </el-select>
</el-form-item>
```

**4. 项目部门树形选择器**
```vue
<el-form-item label="项目部门" prop="projectDept">
  <el-tree-select
    v-model="form.projectDept"
    :data="deptOptions"
    :props="{ value: 'deptId', label: 'deptName', children: 'children' }"
    value-key="deptId"
    placeholder="请选择项目部门"
    check-strictly
  />
</el-form-item>
```

**5. 项目经理（按岗位过滤）**
```vue
<el-form-item label="项目经理" prop="projectManagerId">
  <el-select v-model="form.projectManagerId" filterable placeholder="请选择项目经理">
    <el-option
      v-for="user in projectManagerList"
      :key="user.userId"
      :label="user.nickName"
      :value="user.userId"
    />
  </el-select>
</el-form-item>
```

**6. 市场经理（按岗位过滤）**
```vue
<el-form-item label="市场经理" prop="marketManagerId">
  <el-select v-model="form.marketManagerId" filterable placeholder="请选择市场经理">
    <el-option
      v-for="user in marketManagerList"
      :key="user.userId"
      :label="user.nickName"
      :value="user.userId"
    />
  </el-select>
</el-form-item>
```

**7. 参与人员多选（标签展示）⭐ 重点**
```vue
<el-form-item label="参与人员" prop="participants">
  <el-select
    v-model="participantIds"
    multiple
    filterable
    placeholder="请选择参与人员"
    style="width: 100%"
  >
    <el-option
      v-for="user in allUserList"
      :key="user.userId"
      :label="user.nickName"
      :value="user.userId"
    />
  </el-select>
  
  <!-- 已选人员标签展示 -->
  <div v-if="participantIds.length > 0" style="margin-top: 10px;">
    <el-tag
      v-for="userId in participantIds"
      :key="userId"
      closable
      @close="handleRemoveParticipant(userId)"
      style="margin-right: 10px; margin-bottom: 10px;"
    >
      {{ getUserName(userId) }}
    </el-tag>
  </div>
</el-form-item>
```

**8. 销售负责人 + 联系方式联动**
```vue
<el-form-item label="销售负责人" prop="salesManagerId">
  <el-select
    v-model="form.salesManagerId"
    filterable
    placeholder="请选择销售负责人"
    @change="handleSalesManagerChange"
  >
    <el-option
      v-for="user in allUserList"
      :key="user.userId"
      :label="user.nickName"
      :value="user.userId"
    />
  </el-select>
</el-form-item>

<el-form-item label="销售联系方式" prop="salesContact">
  <el-input v-model="form.salesContact" readonly placeholder="选择销售负责人后自动填充"/>
</el-form-item>
```

**9. 客户名称搜索**
```vue
<el-form-item label="客户名称" prop="customerId">
  <el-select
    v-model="form.customerId"
    filterable
    remote
    reserve-keyword
    placeholder="请输入客户名称搜索"
    :remote-method="remoteSearchCustomer"
    :loading="customerLoading"
    @change="handleCustomerChange"
  >
    <el-option
      v-for="item in customerList"
      :key="item.customerId"
      :label="item.customerSimpleName"
      :value="item.customerId"
    />
  </el-select>
</el-form-item>
```

**10. 客户联系人 + 联系方式联动**
```vue
<el-form-item label="客户联系人" prop="customerContactId">
  <el-select
    v-model="form.customerContactId"
    placeholder="请选择客户联系人"
    :disabled="!form.customerId"
    @change="handleContactChange"
  >
    <el-option
      v-for="item in contactList"
      :key="item.contactId"
      :label="item.contactName"
      :value="item.contactId"
    />
  </el-select>
</el-form-item>

<el-form-item label="客户联系方式" prop="merchantPhone">
  <el-input v-model="form.merchantPhone" readonly placeholder="选择客户联系人后自动填充"/>
</el-form-item>
```

#### 3.3 添加 data 数据
```javascript
data() {
  return {
    // 用户列表
    allUserList: [],
    projectManagerList: [],
    marketManagerList: [],
    
    // 参与人员
    participantIds: [],
    
    // 部门树
    deptOptions: [],
    
    // 二级区域
    secondaryRegionList: [],
    
    // 客户相关
    customerList: [],
    customerLoading: false,
    contactList: [],
    
    // ... 其他数据
  }
}
```

#### 3.4 添加 methods 方法
```javascript
methods: {
  // 加载用户列表
  async loadUsers() {
    const res = await getAllUsers()
    this.allUserList = res.rows || []
  },
  
  // 加载项目经理列表
  async loadProjectManagers() {
    const res = await getUsersByPostCode('pm')
    this.projectManagerList = res.data || []
  },
  
  // 加载市场经理列表
  async loadMarketManagers() {
    const res = await getUsersByPostCode('scjl')
    this.marketManagerList = res.data || []
  },
  
  // 加载部门树
  async loadDeptTree() {
    const res = await treeselect()
    this.deptOptions = res.data || []
  },
  
  // 一级区域变化
  async handleRegionChange(value) {
    this.form.regionCode = null
    this.secondaryRegionList = []
    if (value) {
      const res = await getSecondaryRegionsByPrimary(value)
      this.secondaryRegionList = res.data || []
    }
  },
  
  // 销售负责人变化
  handleSalesManagerChange(userId) {
    if (userId) {
      const user = this.allUserList.find(u => u.userId === userId)
      this.form.salesContact = user ? user.phonenumber : ''
    } else {
      this.form.salesContact = ''
    }
  },
  
  // 客户变化
  async handleCustomerChange(customerId) {
    this.form.customerContactId = null
    this.form.merchantPhone = ''
    this.contactList = []
    if (customerId) {
      const res = await getContactsByCustomerId(customerId)
      this.contactList = res.data || []
    }
  },
  
  // 客户联系人变化
  handleContactChange(contactId) {
    if (contactId) {
      const contact = this.contactList.find(c => c.contactId === contactId)
      this.form.merchantPhone = contact ? contact.contactPhone : ''
    } else {
      this.form.merchantPhone = ''
    }
  },
  
  // 远程搜索客户
  async remoteSearchCustomer(query) {
    if (query !== '') {
      this.customerLoading = true
      const res = await listCustomer({ customerSimpleName: query })
      this.customerList = res.rows || []
      this.customerLoading = false
    } else {
      this.customerList = []
    }
  },
  
  // 移除参与人员
  handleRemoveParticipant(userId) {
    const index = this.participantIds.indexOf(userId)
    if (index > -1) {
      this.participantIds.splice(index, 1)
    }
  },
  
  // 获取用户名称
  getUserName(userId) {
    const user = this.allUserList.find(u => u.userId === userId)
    return user ? user.nickName : userId
  },
  
  // 打开对话框时加载数据
  handleAdd() {
    this.reset()
    this.loadUsers()
    this.loadProjectManagers()
    this.loadMarketManagers()
    this.loadDeptTree()
    this.open = true
    this.title = "添加项目管理"
  },
  
  // 提交表单前转换参与人员
  submitForm() {
    this.$refs["projectRef"].validate(valid => {
      if (valid) {
        // 将参与人员数组转为逗号分隔字符串
        this.form.participants = this.participantIds.join(',')
        
        if (this.form.projectId != null) {
          updateProject(this.form).then(response => {
            this.$modal.msgSuccess("修改成功")
            this.open = false
            this.getList()
          })
        } else {
          addProject(this.form).then(response => {
            this.$modal.msgSuccess("新增成功")
            this.open = false
            this.getList()
          })
        }
      }
    })
  },
  
  // 编辑时回显参与人员
  handleUpdate(row) {
    this.reset()
    this.loadUsers()
    this.loadProjectManagers()
    this.loadMarketManagers()
    this.loadDeptTree()
    
    const projectId = row.projectId || this.ids
    getProject(projectId).then(response => {
      this.form = response.data
      
      // 将参与人员字符串转为数组
      if (this.form.participants) {
        this.participantIds = this.form.participants.split(',').map(Number)
      } else {
        this.participantIds = []
      }
      
      // 加载二级区域
      if (this.form.region) {
        this.handleRegionChange(this.form.region)
      }
      
      // 加载客户联系人
      if (this.form.customerId) {
        this.handleCustomerChange(this.form.customerId)
      }
      
      this.open = true
      this.title = "修改项目管理"
    })
  }
}
```

---

## ✅ 验收标准

### 功能验收
- [ ] 项目编号在保存时自动生成，格式正确
- [ ] 选择一级区域后，二级区域下拉框自动加载对应数据
- [ ] 项目部门使用树形选择器，可展开/收起
- [ ] 项目经理只显示岗位编码为 pm 的用户
- [ ] 市场经理只显示岗位编码为 scjl 的用户
- [ ] 参与人员支持多选，选中后以标签形式展示
- [ ] 参与人员标签可以单独删除
- [ ] 选择销售负责人后，自动填充联系方式
- [ ] 客户名称支持远程搜索
- [ ] 选择客户后，客户联系人下拉框自动加载
- [ ] 选择客户联系人后，自动填充联系方式

### 用户体验验收
- [ ] 所有下拉框支持搜索过滤
- [ ] 必填字段有红色星号标识
- [ ] 禁用字段显示为灰色
- [ ] 标签删除有确认提示
- [ ] 加载数据时有 loading 提示

---

## 🚀 执行顺序

1. **先做后端** → 确保接口可用
2. **再做前端 API** → 封装接口调用
3. **最后做页面** → 实现 UI 交互

**预计总耗时**: 2 小时

---

你想现在开始执行吗？我可以：
1. 一次性生成所有代码
2. 分步骤逐个实现
3. 先实现某几个关键功能

选择哪种方式？
