# language: zh-CN
# 用途：场景覆盖评审（项目无 Cucumber 工具链，本文件不直接执行；
#       落地映射见 bdd/coverage.md —— 对应 tests/e2e-row-link-new-tab.spec.js 的 Playwright 用例）

功能: 列表页操作列「详情/编辑」可在新标签页打开
  作为需要横向比对多条记录的业务人员（项目经理、市场/销售经理、财务、管理层），
  我希望能像用普通网页链接一样，把多条记录分别开到新标签里同时看，
  而不是「进详情 → 返回 → 找回刚才查到哪 → 再进下一条」。

  背景:
    假如 我已登录且拥有相应模块的 query / edit 权限
    并且 我打开了一个查询列表页，列表中至少有一行数据
    并且 该行的「详情」「编辑」在业务上是跳转到独立页面（不是弹窗）

  # ─────────────────── 一、新标签能力（本次改造的目的） ───────────────────

  场景: 操作列的详情入口是一个真实链接
    当 我查看该行「详情」入口的页面元素
    那么 它是一个 <a> 元素
    并且 它带有 href 属性，值为该条记录的详情路由地址
    # 这是本次改造的本质：没有 href，浏览器就不认为这是链接，
    # 右键菜单不会出现「在新标签页中打开链接」，修饰键与中键也无从生效

  场景: 操作列的编辑入口是一个真实链接
    当 我查看该行「编辑」入口的页面元素
    那么 它是一个 <a> 元素
    并且 它带有 href 属性，值为该条记录的编辑路由地址

  场景: href 指向的记录真实存在
    当 我取出「详情」入口 href 中的记录主键
    并且 用该主键调用后端查询接口
    那么 接口返回成功，且返回记录的主键与 href 中的一致
    # 防止 href 拼成了占位值或错误字段（如把 taskId 写成 projectId）

  场景: 右键可以在新标签页打开
    当 我在「详情」入口上点击鼠标右键
    那么 浏览器上下文菜单出现「在新标签页中打开链接」
    并且 选择该项后，新标签加载出这条记录的详情页

  场景: Ctrl/⌘ + 左键在新标签页打开
    当 我按住 Ctrl（macOS 为 ⌘）并左键点击「详情」
    那么 浏览器打开一个新标签
    并且 新标签的地址是这条记录的详情页
    并且 原来那个标签的地址保持不变
    # 「原标签地址不变」是关键：若代码无差别 preventDefault 后再 router.push，
    # 会出现「既开了新标签、当前页也跳走了」的双重跳转

  场景: 鼠标中键点击在新标签页打开
    当 我用鼠标中键点击「详情」
    那么 浏览器打开一个新标签，地址是这条记录的详情页
    并且 原来那个标签的地址保持不变
    # 注意：中键触发的是 auxclick 而非 click，不经过左键分支

  场景: 编辑入口同样支持中键新标签
    当 我用鼠标中键点击「编辑」
    那么 浏览器打开一个新标签，地址是这条记录的编辑页

  # ─────────────────── 二、原有行为不得破坏（回归红线） ───────────────────

  场景: 普通左键仍走单页跳转，不整页刷新
    当 我用普通左键点击「详情」
    那么 当前标签跳转到这条记录的详情页
    并且 页面没有发生整页重载（单页应用的运行时状态得以保留）
    # 若退化成浏览器原生跳转，整个前端会重新启动，白屏一次且丢失内存态

  场景: 从详情返回列表后，原先的查询条件仍在
    假如 我在列表页设置了若干查询条件并查询出结果
    当 我用普通左键点击「详情」进入详情页
    并且 我返回列表页
    那么 之前的查询条件仍然生效
    # 依赖 onBeforeRouteLeave + sessionStorage 的既有缓存机制，左键走 SPA 路由才会触发

  场景: 无权限时看不到对应入口
    假如 我没有该模块的 query 权限
    当 我查看列表的操作列
    那么 「详情」入口不存在于页面中（是被移除，而非仅隐藏）
    # v-hasPermi 通过删除 DOM 元素实现，组件必须是单根节点才能被正确删除

  场景: 操作列外观与改造前一致
    当 我查看操作列
    那么 按钮的文字、图标、颜色、大小与按钮间距均与改造前相同
    # 采用 el-button 的 tag="a" 而非换用 el-link，即为保证 class 与子结构不变

  场景: 同一操作列的其他按钮不受影响
    当 我查看操作列中「附件」「查看合同」「删除」等其他按钮
    那么 它们的行为与改造前完全一致

  # ─────────────────── 三、边界与不适用情形 ───────────────────

  场景: 详情为弹窗的页面不提供新标签能力
    假如 我打开的是「客户管理」这类详情以弹窗展示的列表页
    当 我查看操作列的「详情」
    那么 它仍是普通按钮，点击后弹出对话框
    # 这类详情没有对应 URL，做成链接会得到一个打开后空无一物的死链

  场景: 目标路由无法解析时退化为普通按钮
    假如 某行数据的路由地址因故无法被解析
    当 页面渲染该行的「详情」
    那么 它退化为普通按钮，点击仍能正常跳转
    并且 页面不报错
    # 功能可用性优先于新标签能力

  场景: 入口处于禁用状态时不是链接
    假如 「编辑」入口按业务规则处于禁用状态
    当 我查看该入口
    那么 它不带 href，且是一个普通按钮
    # 关键约束：Element Plus 的按钮在渲染为链接时禁用态会完全失效，
    # 一个「禁用的 <a href>」被点击会绕过前端拦截直接整页跳转

  # ─────────────────── 四、安全 ───────────────────

  场景大纲: 记录主键含异常内容时不产生危险链接
    假如 某条记录的主键值为 <恶意值>
    当 页面据此生成「详情」入口的 href
    那么 生成的地址仍是本站同源的普通网页地址
    并且 不会构成 javascript: 之类的可执行地址，也不会跳向站外

    例子:
      | 恶意值                          |
      | javascript:alert(1)             |
      | ../../../evil                   |
      | "><img src=x onerror=alert(1)>  |
      | https://evil.example.com        |
      | //evil.example.com              |

  场景: 暴露 URL 不构成越权
    假如 我没有查看某条记录的权限
    当 我直接把该记录的详情地址输入浏览器打开
    那么 服务端拒绝该请求
    # 详情地址本就可由用户手工拼出，href 的存在不新增攻击面；
    # 权限由服务端 @PreAuthorize 独立校验，不依赖前端是否显示入口
