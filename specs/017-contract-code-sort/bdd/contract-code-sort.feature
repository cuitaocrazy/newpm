# language: zh-CN
# 用途：场景覆盖评审（项目无 Cucumber 工具链，本文件不直接执行；
#       落地时按 bdd/coverage.md 的映射转成 Playwright e2e 用例）

功能: 合同管理与付款里程碑列表按合同编号排序
  作为合同与财务口的业务人员，我希望能按合同编号排列这两个列表，
  以便按编码规则成组浏览、对账与批量核查；
  同时排序不能弄乱列表原有的合计行与合同分组显示。

  背景:
    假如 合同管理列表与付款里程碑列表均由后端分页，每页 10 条
    并且 库中合同数量超过一页，且存在合同编号为空的合同
    并且 付款里程碑列表中存在「一个合同挂多条付款里程碑」的数据

  # ───────────────────────── 一、排序控件 ─────────────────────────

  场景大纲: 合同编号表头提供排序图标
    当 业务人员打开<列表>
    那么 「合同编号」表头右侧显示排序图标
    并且 该表头可点击排序

    例子:
      | 列表           |
      | 合同管理       |
      | 付款里程碑     |

  场景大纲: 点击表头按三态循环切换
    假如 业务人员位于<列表>
    当 第一次点击「合同编号」表头
    那么 列表按合同编号升序排列
    当 第二次点击「合同编号」表头
    那么 列表按合同编号降序排列
    当 第三次点击「合同编号」表头
    那么 排序被取消，列表回到该页面的默认排序

    例子:
      | 列表           |
      | 合同管理       |
      | 付款里程碑     |

  # ───────────────────────── 二、排序语义 ─────────────────────────

  场景: 排序作用于全部数据而非当前页
    # 这是本特性的核心语义：列表是后端分页的，前端只持有当页 10 条，
    # 若在前端重排，用户看到的只是「当页内部有序」，跨页仍然是乱的。
    假如 合同管理列表共有多页数据
    当 业务人员按合同编号升序排序
    那么 排序请求被发往后端，携带排序字段与升降序方向
    并且 第一页与第二页的记录互不重复

  场景大纲: 排序键存在并列值时翻页仍不重复不遗漏
    # Issue #16：本场景在实现后首次执行即失败，暴露出既有缺陷——
    # 排序键并列时 MySQL 不保证并列行的顺序，每页 limit 各自排序会导致
    # 同一条记录既落进第 1 页又落进第 2 页，同时另一些记录两页都进不去。
    # 修复：排序时追加唯一次级键 c.contract_id，使排序成为全序。
    假如 合同管理列表中<排序列>存在大量并列值
    当 业务人员按该列排序并翻到第二页
    那么 第二页的记录不与第一页重复
    并且 逐页翻阅可看到全部记录，无一遗漏

    例子:
      | 排序列           | 并列行数 |
      | 合同编号         | 143      |
      | 合同签订日期     | 133      |
      | 合同金额         | 107      |

  场景大纲: 升序与降序给出相反的排列
    假如 业务人员位于<列表>
    当 分别以升序与降序请求第一页
    那么 两次返回的首条记录不是同一条

    例子:
      | 列表           |
      | 合同管理       |
      | 付款里程碑     |

  场景: 合同编号为空的记录参与排序而不被丢弃
    假如 库中存在合同编号为空的合同
    当 业务人员按合同编号升序排序
    那么 空编号的记录排在最前
    并且 这些记录仍然出现在列表中

  场景: 排序与查询条件叠加生效
    假如 业务人员已按某个合同状态筛选出一批数据
    当 在该筛选结果上按合同编号排序
    那么 返回的记录仍然全部满足该筛选条件
    并且 记录总数与排序前一致

  场景: 翻页时保持当前排序
    假如 业务人员已按合同编号降序排序
    当 翻到第二页
    那么 第二页仍按合同编号降序排列
    并且 第二页的记录不与第一页重复

  # ───────────────── 三、不得破坏的既有显示（不变式）─────────────────

  场景: 排序不拆散同一合同的多条付款里程碑
    # INV-1：该查询是「合同 1:N 付款」的嵌套 resultMap，PageHelper 会替换
    # mapper 中原有的 order by。若同一合同的多行被打散，前端 spanMethod
    # 的单元格合并会错乱成「同一合同出现多次、每次只带一条付款」。
    假如 付款里程碑列表中某合同挂有多条付款里程碑
    当 业务人员按合同编号升序或降序排序
    那么 该合同在返回结果中仍是一条记录，其下挂着全部付款里程碑
    并且 列表中同一合同的合并单元格显示正常

  场景大纲: 合计行始终固定在首行且不参与排序
    # INV-2：合计行由前端 computed 以 [合计, ...数据] 拼接，与后端排序无关
    假如 业务人员位于<列表>
    当 按合同编号升序或降序排序
    那么 「合计」行仍位于列表第一行

    例子:
      | 列表           |
      | 合同管理       |
      | 付款里程碑     |

  # ───────────────────────── 四、安全 ─────────────────────────

  场景: 拒绝伪造的排序字段
    # INV-3：orderByColumn 是被拼进 order by 的用户可控字符串
    当 攻击者构造一个含 SQL 语句的排序字段发起查询
    那么 请求被拒绝
    并且 提示参数不符合规范
    并且 数据库未发生任何变更

  场景: 排序字段限于白名单内的合法字段名
    当 以合法字段名 contract_code 作为排序字段查询
    那么 查询正常返回结果
