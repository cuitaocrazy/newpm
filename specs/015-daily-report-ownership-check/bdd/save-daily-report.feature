# language: zh-CN
# 用途：场景覆盖评审（项目无 Cucumber 工具链，本文件不直接执行；
#       落地时按 bdd/coverage.md 的映射转成 JUnit 单测与 Playwright e2e）

功能: 保存日报时的工时保护与项目归属校验
  作为填报人，我希望保存日报时不会弄丢我看不见的工时；
  作为管理者，我希望没有人能把工时填到与自己无关的项目上。

  背景:
    假如 填报人"张三"不在"无需填写日报"白名单中
    并且 项目"在建A"处于在建状态且张三是其成员
    并且 项目"已结项B"已结项，张三曾是其成员
    并且 项目"无关C"在建，但张三从未以任何身份参与过

  # ───────────────────────── 一、工时保护（防丢失）─────────────────────────

  场景: 修改可见项目时，不可见项目的工时必须原样保留
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时与"已结项B" 2 小时
    并且 填写页只显示"在建A"（"已结项B"不在可填列表中）
    当 张三把"在建A"改为 6 小时并保存（提交内容只含"在建A"）
    那么 保存成功
    并且 该日报仍有两条工时："在建A" 6 小时与"已结项B" 2 小时
    并且 "已结项B"的实际人天保持不变

  场景: 被保留的工时其全部字段逐字不变
    假如 张三在"2026-07-20"的日报中"已结项B"一条工时为 2 小时、内容"收尾联调"、挂任务"T-9"、类别"开发"
    当 张三修改同一天"在建A"的工时并保存
    那么 "已结项B"那条记录的工时数、工作内容、所属任务、工作类别与保存前完全相同

  场景: 填报人主动清零可见项目的工时，该记录应被删除
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时
    当 张三把"在建A"的工时清零并保存（提交内容不含"在建A"）
    那么 保存成功
    并且 该日报中不再有"在建A"的工时记录

  场景: 假期记录在所有入口均始终可见，故未提交即视为主动删除
    # 已核查：桌面与移动端的回填条件均为 entryType !== 'work' 的正向排除，
    # 会回填字典 sys_rbtype 全部 8 种类型，不存在「看不见的假期记录」
    # （sys_rbtype 含 work/leave/comp/annual/marriage/maternity/bereavement/8-出差途中）
    假如 张三在"2026-07-20"的日报中已有"年假" 8 小时
    当 张三删除该假期记录并保存（提交内容不含任何假期）
    那么 保存成功
    并且 该日报中不再有假期记录

  场景: 修改项目工时不影响同一天的假期记录
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时与"出差途中" 8 小时
    当 张三把"在建A"改为 6 小时并保存（提交内容含"在建A"与"出差途中"两条）
    那么 保存成功
    并且 "出差途中" 8 小时保持不变

  场景: 提交空内容时，只清空可见范围
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时与"已结项B" 2 小时
    当 张三提交一个不含任何工时记录的保存请求
    那么 保存成功
    并且 "在建A"的工时被清除
    并且 "已结项B" 2 小时仍然保留

  场景: 填报人当前没有任何可填项目时，既有项目工时全部受保护
    假如 张三名下所有项目都已结项，可填项目列表为空
    并且 他在"2026-07-20"的日报中已有"已结项B" 2 小时与"年假" 8 小时
    当 张三提交一个只含"年假" 8 小时的保存请求
    那么 保存成功
    并且 "已结项B" 2 小时仍然保留

  场景: 同一项目下多个任务的工时按记录粒度处理
    假如 项目"在建A"下有任务"T-1"与"T-2"，张三在"2026-07-20"各填了 4 小时
    当 张三把"T-1"清零、"T-2"改为 6 小时并保存（提交内容只含 T-2）
    那么 保存成功
    并且 "T-1"的工时记录被删除
    并且 "T-2"的工时为 6 小时

  场景: 首次保存某天日报时不涉及保护逻辑
    假如 张三在"2026-07-25"尚无任何日报
    当 张三提交"在建A" 8 小时
    那么 保存成功
    并且 该日报仅含这一条工时

  # ───────────────────────── 二、归属校验（防越权写入）─────────────────────

  场景: 拒绝把工时填到从未参与过的项目上
    当 张三提交包含"无关C" 1 小时的日报
    那么 保存被拒绝
    并且 提示信息包含项目名称"无关C"
    并且 "无关C"的实际人天保持不变
    并且 该日报的既有工时记录未发生任何变化

  场景: 合法项目与无关项目混合提交时，整次保存被拒绝
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时
    当 张三提交"在建A" 6 小时与"无关C" 1 小时两条记录
    那么 保存被拒绝
    并且 "在建A"的工时仍为 4 小时（未被部分写入）
    并且 "在建A"与"无关C"的实际人天均保持不变

  场景: 拒绝发生时，不可见的合法工时必须仍然存在
    # 缺口 3：INV-1（拒绝时零变更）与 FR-001（防丢失）的交叉点。
    # 若实现把校验放在删除之后，会出现「校验失败了、但工时已经被删了」，比现状更糟。
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时与"已结项B" 2 小时
    当 张三提交"在建A" 6 小时与"无关C" 1 小时（含一条非法记录）
    那么 保存被拒绝
    并且 "已结项B" 2 小时仍然存在
    并且 "在建A"的工时仍为 4 小时
    并且 三个项目的实际人天均未发生变化

  场景: 离场成员仍可维护自己在该项目上的历史工时
    假如 项目"在建D"在建，张三曾是其成员但现已离场
    并且 张三在"2026-07-20"的日报中已有"在建D" 4 小时
    当 张三把"在建D"改为 6 小时并保存
    那么 保存成功
    并且 "在建D"的工时为 6 小时

  场景: 拒绝指向已被删除项目的工时
    当 张三提交一条指向已被删除项目的工时
    那么 保存被拒绝
    并且 提示信息说明该项目不在其参与范围内

  场景: 假期记录不受项目归属校验约束
    当 张三提交一条"请事假" 8 小时的记录（不关联任何项目）
    那么 保存成功

  场景: 拒绝把工时挂到不属于该项目的任务上
    假如 任务"T-9"隶属于项目"在建D"
    当 张三提交一条声明项目为"在建A"、任务为"T-9"的工时
    那么 保存被拒绝
    并且 提示信息说明任务与所选项目不匹配

  场景: 拒绝事件可被追溯
    当 张三提交一个会被拒绝的保存请求
    那么 操作日志中存在一条对应记录
    并且 该记录包含操作人、完整请求参数、失败状态与拒绝原因

  # ───────────────────────── 三、结项校验 ──────────────────────────────────

  场景: 拒绝为已结项项目新增工时
    假如 张三在"2026-07-25"尚无任何日报
    当 张三提交"已结项B" 4 小时
    那么 保存被拒绝
    并且 提示信息说明项目已结项

  场景: 拒绝修改已结项项目的既有工时
    假如 张三在"2026-07-20"的日报中已有"已结项B" 2 小时
    当 张三提交"已结项B" 6 小时（经接口直接调用，绕过填写页）
    那么 保存被拒绝
    并且 "已结项B"的工时仍为 2 小时

  场景: 项目在填报过程中结项时给出明确提示
    假如 张三打开"2026-07-25"的填写页时项目"在建A"仍可填
    并且 在他保存之前"在建A"被变更为已结项
    当 张三提交"在建A" 8 小时
    那么 保存被拒绝
    并且 提示信息说明项目已结项（而非静默失败或静默丢弃）

  # ────────── 三点五、删除整条日报时的工时保护（业务方 2026-07-30 确认纳入）──────

  场景: 删除整条日报时，不可见项目的工时不得被连带删除
    # 业务规则：项目结项后不再显示在日报中，但此前已填的数据必须保留——
    # 不能因为填报人在日报上执行了删除操作，就把已结项项目的工时无辜删掉。
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时与"已结项B" 2 小时
    当 张三删除整条日报
    那么 "在建A"的 4 小时被删除
    并且 "已结项B"的 2 小时仍然保留
    并且 "已结项B"的实际人天保持不变

  场景: 有工时被保留时，日报主记录必须保留且汇总工时重算
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时与"已结项B" 2 小时
    当 张三删除整条日报
    那么 该日报主记录仍然存在（否则被保留的工时将无主记录可归属）
    并且 该日报主记录的汇总工时被重算为 2 小时

  场景: 无工时需保留时，日报主记录应被删除
    假如 张三在"2026-07-20"的日报中只有"在建A" 4 小时（全部可见）
    当 张三删除整条日报
    那么 该日报的全部工时被删除
    并且 该日报主记录被删除，不留残余

  场景: 只含假期记录的日报可被完整删除
    假如 张三在"2026-07-20"的日报中只有"年假" 8 小时
    当 张三删除整条日报
    那么 假期记录被删除
    并且 该日报主记录被删除

  # ───────────────────────── 四、不变式与一致性 ────────────────────────────

  场景: 保存成功后项目实际人天与明细汇总严格相等
    假如 项目"在建A"当前实际人天与其全部日报明细汇总相等
    当 张三在任意一天新增、修改或清零"在建A"的工时并保存成功
    那么 "在建A"的实际人天仍严格等于其全部日报明细的工时汇总

  场景: 保护动作不改变被保留项目的实际人天
    假如 "已结项B"的实际人天为 X
    当 张三修改同一天其他项目的工时并保存成功（"已结项B"的工时被保留）
    那么 "已结项B"的实际人天仍为 X

  场景: 保存被拒绝时数据库状态完全不变
    假如 记录下保存前该日报的全部明细与相关项目的实际人天
    当 张三提交一个会被拒绝的保存请求
    那么 该日报的全部明细与相关项目的实际人天与保存前逐一相同

  场景: 系统中不存在孤立的工时记录
    当 对任意日报执行保存或删除操作后
    那么 不存在「有工时记录但无对应日报主记录」的数据
    并且 不存在「日报主记录的汇总工时与其明细之和不符」的情形

  # ───────────────────────── 五、多入口一致性 ──────────────────────────────

  场景大纲: 各写入入口均受同等保护
    假如 张三在"2026-07-20"的日报中已有"在建A" 4 小时与"已结项B" 2 小时
    当 张三通过<入口>把"在建A"改为 6 小时并保存
    那么 "已结项B" 2 小时仍然保留

    例子:
      | 入口          |
      | 桌面填写页    |
      | 移动端 H5     |
      | 直接调用接口  |
