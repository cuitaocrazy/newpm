# language: zh-CN
# 用途：场景覆盖评审（项目无 Cucumber 工具链，本文件不直接执行；
#       落地时按 bdd/coverage.md 的映射转成 JUnit 单测与 Playwright e2e）

功能: 合同编号在系统中不重复
  作为合同录入人员，我希望在我键入一个别人已经用过的合同编号时系统当场拦住我，
  以便我立刻回去核对纸质合同，而不是把两份同号合同一起存进台账；
  作为财务与合同管理岗，我希望按一个编号只能查到一份在用合同，
  以便对账时不必人工猜"哪一份才是真的"；
  作为合同录入人员，我同时希望还没拿到正式编号的合同照样能先建起来，
  以便框架协议与在途合同不被"必须填编号"卡住。

  背景:
    假如 合同编号在本系统中一直是选填项，台账里本来就有大量没有编号的合同
    并且 合同"甲方框架协议"已经在用，其合同编号是"4004-2024-1210-01-00018"
    并且 "甲方框架协议"由部门"项目一组"的录入人员建立
    并且 当前登录的是部门"项目二组"的录入人员，他看不到"项目一组"的合同列表
    并且 台账里另有一份在用合同"历史采购合同"，其合同编号是"ABC-001"

  # ═══════════════════ 一、重复编号被当场拦住（核心）═══════════════════

  场景大纲: 键入已被占用的合同编号时保存被拒绝
    # 新增与编辑是两个独立入口，两处都必须拦。历史上两处都没有任何校验。
    假如 录入人员打开合同<入口>页面
    并且 已填好合同名称、部门、客户、合同类型、合同状态、合同金额
    当 在"合同编号"里键入"4004-2024-1210-01-00018"并保存
    那么 保存不成功，页面提示"合同编号已存在"
    并且 提示里带出这个编号本身，录入人员知道是哪一个编号撞了
    并且 台账里仍然只有一份编号为"4004-2024-1210-01-00018"的在用合同

    例子:
      | 入口 |
      | 新增 |
      | 编辑 |

  场景: 编号在离开输入框时就给出反馈，不用等到点保存
    假如 录入人员正在新增合同
    当 在"合同编号"里键入"4004-2024-1210-01-00018"并把光标移开
    那么 该输入框下方立即出现红色提示"合同编号已存在"
    并且 此时点击保存按钮，表单不会提交

  场景: 编辑时把编号改成别人的编号，同样被拒绝
    假如 录入人员正在编辑合同"某新合同"，它原本没有编号
    当 把合同编号改成"4004-2024-1210-01-00018"并保存
    那么 保存不成功，提示"合同编号已存在"
    并且 "某新合同"的编号仍然是空的，没有被改写

  场景: 拦截是硬的，没有"确认后仍然保存"的出口
    # 业务决策 D-C：不提供任何后门，也不提供管理员豁免。
    假如 录入人员键入了一个已被占用的编号并被拦下
    当 录入人员再次点击保存
    那么 依然被拒绝，界面上不出现"仍要保存""确认继续"之类的按钮
    并且 无论以什么身份登录，都没有可以放行这次保存的开关

  场景: 被拦下时提示是给人看的，不是给程序员看的
    假如 录入人员键入了一个已被占用的编号
    当 保存被拒绝
    那么 提示是一句能读懂的中文，说明编号已存在
    并且 提示中不出现异常堆栈、SQL 语句或表名字段名

  @待确认
  场景: 提示里带出冲突的是哪一份合同
    # ⚠️ 本场景对应 spec.md §八「错误提示文案未定稿」这条待确认项，尚未拍板。
    #   已定的下限是"提示含被占用的编号本身"（上面的场景已覆盖）。
    #   是否再带上冲突合同的名称/所属部门，涉及跨部门信息可见性——判重刻意不受
    #   数据权限约束，带出对方合同名等于让部门二组看见部门一组的合同名称。
    #   业务方确认前，本场景不落地为断言（见 coverage.md 状态列）。
    假如 编号"4004-2024-1210-01-00018"已被合同"甲方框架协议"占用
    当 录入人员用同一个编号新增合同并保存
    那么 提示中出现占用该编号的合同名称"甲方框架协议"
    并且 录入人员据此判断出这是别的部门已经录过的同一份合同

  # ═══════════════════ 二、不填编号照样能干活 ═══════════════════

  场景: 不填合同编号可以正常保存
    # 业务决策 D-A：编号保持非必填。台账里已有一百多份没有编号的在用合同。
    假如 录入人员正在新增一份还没拿到编号的框架协议
    并且 除合同编号外的必填项都已填好
    当 直接保存，合同编号留空
    那么 保存成功，合同出现在台账中
    并且 界面上不出现"合同编号已存在"之类的提示
    并且 界面上也不出现"请输入合同编号"这样的必填提示

  场景: 多份没有编号的合同可以同时存在
    假如 台账中已有若干份合同编号为空的在用合同
    当 录入人员再新增一份不填编号的合同并保存
    那么 保存成功
    并且 这些空编号的合同彼此之间不被判为重复

  场景大纲: 这些输入等同于"没有填编号"
    # 归一化口径：删掉所有 TAB/回车/换行 → 去掉首尾空格 → 结果为空或字面"无"即视为未填。
    #   字面"无"来自现实数据——有录入人员用中文"无"表达"没有编号"。
    假如 录入人员正在新增合同，其余必填项已填好
    当 在"合同编号"里输入<输入内容>并保存
    那么 保存成功
    并且 这份合同在台账里显示为没有编号
    并且 它不与任何其他合同构成编号重复

    例子:
      | 说明        | 输入内容     |
      | 什么都不填  | （留空）     |
      | 只敲了空格  | "   "        |
      | 只敲了制表符| "\t"         |
      | 空格加制表符| " \t "       |
      | 中文的"无"  | "无"         |

  场景: 把已有的编号清空并保存，编号真的被清掉了
    # 这是"提示保存成功但值不变"这类顽固缺陷的高发点，必须单独验证。
    假如 合同"某合同"当前的编号是"E2E-DEMO-001"
    当 录入人员在编辑页把编号删空并保存
    那么 提示保存成功
    并且 重新打开这份合同，编号显示为空，不是原来的"E2E-DEMO-001"

  # ═══════════════════ 三、看起来一样的，就是一样的 ═══════════════════

  场景大纲: 只差看不见的空白，仍算同一个编号
    # 现实依据：台账里有一条记录的编号末尾带一个制表符，肉眼与另一条完全一样，
    #   在任何朴素的字符串比较下都不相等——这正是两份同号合同能并存的原因之一。
    假如 编号"4004-2024-1210-01-00018"已被在用合同占用
    当 录入人员键入<键入内容>并保存
    那么 保存被拒绝，提示"合同编号已存在"

    例子:
      | 说明          | 键入内容                      |
      | 前面多个空格  | "  4004-2024-1210-01-00018"   |
      | 后面多个空格  | "4004-2024-1210-01-00018  "   |
      | 末尾一个制表符| "4004-2024-1210-01-00018\t"   |
      | 开头一个制表符| "\t4004-2024-1210-01-00018"   |

  场景: 编号中间的空格不会被抹掉
    # 归一化只去首尾空白与控制字符，不动中间的空格——否则会把两个真正不同的编号误判成一个。
    假如 编号"ABC 001"已被在用合同占用
    当 录入人员键入"ABC001"并保存
    那么 保存成功，这是两个不同的编号

  场景: 存进去的编号是干净的
    假如 录入人员从别处复制粘贴，编号末尾意外带上了制表符
    当 保存成功后重新打开这份合同
    那么 显示的编号不再带有制表符和首尾空格
    并且 今后不会再产生"看起来一样、实际不一样"的编号

  # ═══════════════════ 四、不能误伤正常操作 ═══════════════════

  场景: 编号是别人编号的前缀时，不算重复
    # 现有校验接口走的是列表的模糊搜索，输 ABC 会命中 ABC-001 而误报"已存在"。
    假如 编号"ABC-001"已被在用合同"历史采购合同"占用
    当 录入人员新增合同并键入编号"ABC"
    那么 保存成功，不出现"合同编号已存在"

    # 反向同理
    假如 编号"ABC"已被占用
    当 录入人员键入"ABC-002"
    那么 保存成功

  场景: 编辑时不动编号，直接保存不会被自己拦住
    # 判重必须排除合同自身，否则台账里每一份有编号的合同都再也改不动了。
    假如 合同"甲方框架协议"的编号是"4004-2024-1210-01-00018"
    当 录入人员打开它的编辑页，只把合同金额从 100 万改成 120 万，编号原样不动，然后保存
    那么 保存成功
    并且 编号仍然是"4004-2024-1210-01-00018"

  场景: 台账里现有的每一份合同都还能正常编辑保存
    假如 台账中有 300 多份在用合同，其中一百多份没有编号
    当 逐份打开编辑页原样保存
    那么 每一份都保存成功，没有任何一份被编号判重卡住

  场景: 按合同编号模糊搜索照常可用
    # 判重要精确，搜索要模糊——两件事，不能互相牵连。
    假如 台账里有编号为"ABC-001""ABC-002"的合同
    当 录入人员在合同列表的"合同编号"里输入"ABC"并查询
    那么 两份合同都被搜出来
    并且 搜索结果与本次改造前一致

  场景: 删除合同时不会被编号重复挡住
    # 台账里现存若干组"同一个编号多行"的历史脏数据，删除是清理它们的手段，
    #   如果删除动作本身要过判重，这些数据就永远删不掉。
    假如 台账里因历史原因存在两份编号相同的在用合同
    当 合同管理岗把其中一份删除
    那么 删除成功
    并且 另一份不受影响

  # ═══════════════════ 五、删除之后编号可以再用 ═══════════════════

  场景: 已删除合同占用过的编号可以被重新使用
    # "录错了删掉重录"是正常业务动作，不能被系统拒绝。
    假如 合同"录错的合同"编号为"E2E-REUSE-001"，已被删除
    当 录入人员新增一份合同，编号同样填"E2E-REUSE-001"
    那么 保存成功
    并且 台账里只有这一份在用的"E2E-REUSE-001"

  场景: 被删除的合同仍保留它原来的编号
    # 删除是软删除，记录留作审计追溯，不因为要加约束就去改写它的编号字面值。
    假如 合同"录错的合同"编号为"E2E-REUSE-001"，已被删除
    当 管理员在后台查看这份已删除的记录
    那么 它的编号仍然是"E2E-REUSE-001"，没有被清空或改写

  # ═══════════════════ 六、跨部门与最后一道兜底 ═══════════════════

  场景: 别的部门录过的编号，同样能被检出
    # 这是本特性最关键的一条：实际发生的那对重复，两份合同分属两个部门，
    #   任何按部门收窄的查询都看不见对方——判重必须跳出数据权限的视野。
    假如 编号"4004-2024-1210-01-00018"被部门"项目一组"的合同占用
    并且 当前录入人员属于部门"项目二组"，在合同列表里根本看不到那份合同
    当 他新增合同并键入同一个编号
    那么 保存被拒绝，提示"合同编号已存在"

  场景: 判重不会顺带泄露别的部门的合同内容
    假如 部门"项目二组"的录入人员因编号重复被拦下
    当 他查看返回的提示与页面
    那么 他看不到对方合同的金额、客户、签订日期等任何业务内容
    并且 他在合同列表里依然看不到那份合同

  场景: 绕过界面直接写库，也会被数据库挡住
    # 界面校验拦得住人，拦不住并发写入与直连 SQL 的旁路。
    假如 编号"4004-2024-1210-01-00018"已被在用合同占用
    当 有人绕过页面，直接往合同表里插入一条同编号的在用记录
    那么 数据库拒绝这次写入
    并且 台账里仍然只有一份该编号的在用合同

  场景: 两个人同时提交同一个编号，只有一个能成
    假如 两名录入人员在同一时刻用同一个新编号提交新增
    当 两次提交几乎同时到达
    那么 只有一份合同被保存下来
    并且 另一人看到的是与手工重复时同一句提示"合同编号已存在"，而不是系统错误
